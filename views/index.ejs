<!DOCTYPE html>
<html lang="ar" dir="rtl">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>KICK HUB | نيون بلو</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>

    <!-- الخلفية المتحركة GIF -->
    <div class="animated-bg"></div>

    <header class="main-header">
        <button class="plus-btn" onclick="openModal()">+ أضف قناتك</button>
    </header>

    <!-- شريط الإحصائيات العلوي -->
    <div class="stats-bar" style="text-align: center; padding: 15px; color: #888; position: relative; z-index: 1;">
        إجمالي القنوات: <b>
            <%= stats.totalStreamers %>
        </b> |
        متصل الآن: <b style="color: var(--neon-blue);">
            <%= stats.liveNow %>
        </b>
    </div>

    <!-- حاوية البطاقات بنظام "النيون سليم" -->
    <div class="cards-grid">
        <% streamers.forEach(streamer=> { %>
            <div class="card">

                <!-- اليمين: الصورة المصغرة والعداد -->
                <div class="profile-wrapper">
                    <img src="<%= streamer.profilePic || '/black.png' %>" class="profile-img">
                    <div class="viewers-tag">VIEWERS <b>
                            <%= streamer.viewers %>
                        </b></div>
                </div>

                <!-- اليسار: التفاصيل والزر -->
                <div class="card-content">
                    <div class="kick-label">KICK</div>
                    <h3>
                        <%= streamer.kickUsername %>
                    </h3>

                    <% if(streamer.isLive) { %>
                        <div class="live-status">
                            <span class="dot"></span> LIVE
                        </div>
                        <% } %>

                            <a href="https://kick.com<%= streamer.kickUsername %>" target="_blank" class="watch-btn">
                                مشاهدة البث
                            </a>
                </div>

            </div>
            <% }) %>
    </div>

    <!-- نافذة الإضافة (Modal) -->
    <div id="addModal" class="modal-overlay"
        style="display:none; position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.9); backdrop-filter:blur(10px); z-index:2000; align-items:center; justify-content:center;">
        <div
            style="background:#111; padding:30px; border-radius:20px; border:1px solid var(--neon-blue); width:320px; text-align:center;">
            <h2 style="color:var(--neon-blue); font-size: 20px;">انضم للمنصة</h2>
            <form action="/apply" method="POST">
                <input type="text" name="kickUser" placeholder="اسمك في Kick" required
                    style="width:100%; padding:10px; margin:8px 0; background:#222; border:none; color:white; border-radius:8px; box-sizing: border-box;">
                <input type="text" name="discordName" placeholder="حساب الديسكورد"
                    style="width:100%; padding:10px; margin:8px 0; background:#222; border:none; color:white; border-radius:8px; box-sizing: border-box;">
                <button type="submit"
                    style="width:100%; margin-top:15px; background:var(--neon-blue); color:black; border:none; padding:10px; border-radius:8px; font-weight:bold; cursor:pointer;">إرسال
                    الآن</button>
                <button type="button" onclick="closeModal()"
                    style="margin-top:10px; background:none; color:gray; border:none; cursor:pointer; font-size: 12px;">إلغاء</button>
            </form>
        </div>
    </div>

    <script>
        function openModal() { document.getElementById('addModal').style.display = 'flex'; }
        function closeModal() { document.getElementById('addModal').style.display = 'none'; }
    </script>
</body>

</html>